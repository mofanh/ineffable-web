# 系统管理单列管理页重构 TODO

目标：将模型管理、密钥管理、套餐管理、用户管理统一重构为 `AGENTS.md` 中定义的单列管理页模板。

统一模板：

```text
页面标题区
指标区
工具条
主列表 / 表格
行内展开详情 / 折叠区 / 弹窗编辑
```

## Phase 0: app 底座组件

- [x] 新增 `AppListToolbar`，统一搜索、筛选、刷新、新增按钮布局。
- [x] 新增 `AppExpandablePanel`，统一行内展开详情样式。
- [x] 新增 `AppDisclosureSection`，统一弹窗和详情里的折叠分组。
- [x] 新增 `AppFieldGrid`，统一表单字段分组排布。
- [x] 导出组件并保证 lint/build 通过。

## Phase 1: 模型管理

- [ ] 将卡片网格改为单列表格。
- [ ] 增加行内展开技术详情。
- [ ] 编辑弹窗按基础信息、上游连接、上下文限制、能力开关、计费排序分组。
- [ ] 工具条使用 `AppListToolbar`。
- [ ] 更新 TODO 并提交 `refactor(...)` commit。

## Phase 2: 密钥管理

- [ ] 将卡片网格改为单列表格或紧凑列表。
- [ ] 增加行内展开安全详情。
- [ ] 编辑弹窗加入安全说明和字段分组。
- [ ] 工具条使用 `AppListToolbar`。
- [ ] 更新 TODO 并提交 `refactor(...)` commit。

## Phase 3: 套餐管理

- [ ] 移除左右分栏。
- [ ] 将套餐列表改为单列表格。
- [ ] 将模型权限配置改为行内展开区或大弹窗。
- [ ] 保持 visible 关闭时 usable 自动关闭并禁用。
- [ ] 更新 TODO 并提交 `refactor(...)` commit。

## Phase 4: 用户管理

- [ ] 移除左右分栏。
- [ ] 将用户列表改为单列表格。
- [ ] 将套餐记录、用量、权限摘要放入行内展开详情。
- [ ] 编辑角色和套餐分配继续走弹窗。
- [ ] 更新 TODO 并提交 `refactor(...)` commit。

## 验收

- [ ] 四个页面均无左右分栏。
- [ ] 主视图均为列表或表格。
- [ ] 详情均通过行内展开、折叠区或弹窗展示。
- [ ] 不新增页面内一次性通用组件。
- [ ] `npm run lint` 通过。
- [ ] `npm run build` 通过。
