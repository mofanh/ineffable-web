# 系统管理单列管理页重构 TODO

目标：将模型管理、密钥管理、套餐管理、用户管理统一重构为 `AGENTS.md` 中定义的单列管理页模板。

统一模板：

```text
页面标题区
指标区
工具条
主列表 / 表格
行内展开详情 / 折叠区 / 弹窗编辑
```

## Phase 0: app 底座组件

- [x] 新增 `AppListToolbar`，统一搜索、筛选、刷新、新增按钮布局。
- [x] 新增 `AppExpandablePanel`，统一行内展开详情样式。
- [x] 新增 `AppDisclosureSection`，统一弹窗和详情里的折叠分组。
- [x] 新增 `AppFieldGrid`，统一表单字段分组排布。
- [x] 导出组件并保证 lint/build 通过。

## Phase 1: 模型管理

- [x] 将卡片网格改为单列表格。
- [x] 增加行内展开技术详情。
- [x] 编辑弹窗按基础信息、上游连接、上下文限制、能力开关、计费排序分组。
- [x] 工具条使用 `AppListToolbar`。
- [x] 更新 TODO 并提交 `refactor(...)` commit。

## Phase 2: 密钥管理

- [x] 将卡片网格改为单列表格或紧凑列表。
- [x] 增加行内展开安全详情。
- [x] 编辑弹窗加入安全说明和字段分组。
- [x] 工具条使用 `AppListToolbar`。
- [x] 更新 TODO 并提交 `refactor(...)` commit。

## Phase 3: 套餐管理

- [x] 移除左右分栏。
- [x] 将套餐列表改为单列表格。
- [x] 将模型权限配置改为行内展开区或大弹窗。
- [x] 保持 visible 关闭时 usable 自动关闭并禁用。
- [x] 更新 TODO 并提交 `refactor(...)` commit。

## Phase 4: 用户管理

- [x] 移除左右分栏。
- [x] 将用户列表改为单列表格。
- [x] 将套餐记录、用量、权限摘要放入行内展开详情。
- [x] 编辑角色和套餐分配继续走弹窗。
- [x] 更新 TODO 并提交 `refactor(...)` commit。

## Phase 5: 密钥安全轮换

- [x] 编辑已有密钥时锁定 `secret_ref`，避免“改名”意外创建新的引用。
- [x] 新增密钥必须填写密钥值；编辑时留空表示只更新状态/metadata，不轮换密钥。
- [x] 继续保持旧密钥不回显；明文 reveal 需要后端加密存储、管理员二次认证、短时授权和审计日志后另行实现。

## 验收

- [x] 四个页面均无左右分栏。
- [x] 主视图均为列表或表格。
- [x] 详情均通过行内展开、折叠区或弹窗展示。
- [x] 不新增页面内一次性通用组件。
- [x] `npm run lint` 通过。
- [x] `npm run build` 通过。
