# 系统管理页面设计交接文档

本文档用于配合当前界面截图交给设计师，目标是优化系统管理模块的界面结构、视觉一致性和可复用组件规范。

这次不是单页美化，而是一次系统管理模块的产品化设计梳理。设计输出需要能落到现有前端架构里，并沉淀为可复用的底层组件能力。

## 设计范围

本次覆盖四个页面：

- 模型管理：`/system/models`
- 套餐管理：`/system/plans`
- 密钥管理：`/system/secrets`
- 用户管理：`/system/users`

这四个页面都属于 `System Management`，需要作为同一组管理后台页面统一设计，而不是分别单独设计。

## 当前页面定位

### 模型管理

用于维护模型档案，包括：

- 模型展示名
- endpoint 类型
- 上游模型名
- base URL
- API key 引用
- context window
- max output tokens
- 模型能力开关，如 tool、reasoning、vision
- 使用倍率
- 启用状态
- 排序

核心问题是字段多、参数密度高，需要清晰分组。

### 套餐管理

用于维护套餐和模型权限，包括：

- 套餐基础信息
- 月额度
- 是否启用
- 每个套餐可见/可用的模型权限
- 模型访问参数，如倍率、单次 token 限制、每日请求限制

核心问题是“套餐列表”和“模型权限矩阵”之间的关系需要更清楚。

### 密钥管理

用于维护上游模型密钥引用，包括：

- secret ref
- secret value
- status
- 是否已有密钥值

核心问题是安全感和状态表达，需要明确“密钥值不会回显”“留空不修改密钥”等提示。

### 用户管理

用于维护用户、套餐分配和用量，包括：

- 用户列表
- 用户角色
- 套餐分配
- 用户用量
- 用户状态

核心问题是信息层级较复杂，需要在用户、套餐、用量之间建立清晰的主从关系。

## 设计目标

1. 统一四个页面的信息架构。
2. 提升管理后台的扫描效率和操作效率。
3. 减少页面之间视觉和交互模式差异。
4. 将可复用模式沉淀到底层组件，而不是做单页特殊样式。
5. 保持高信息密度，但避免拥挤、遮挡和失焦。
6. 让新增、编辑、删除、搜索、筛选、状态展示都有一致规则。

## 已定版布局方向

本次系统管理模块采用统一的单列管理页模板，不做左右分栏。

统一结构：

```text
页面标题区
指标区
工具条
主列表 / 表格
行内展开详情 / 折叠区 / 弹窗编辑
```

设计规则：

- 主视图永远是列表或表格。
- 详情不常驻右侧，不做左右两块布局。
- 轻量详情通过行内展开或折叠区展示。
- 新增和编辑通过弹窗展示。
- 危险操作通过确认弹窗展示。
- 长配置项可以放在展开区或大弹窗里。

四个页面对应：

- 模型管理：表格 + 行内展开技术详情 + 弹窗编辑
- 套餐管理：表格 + 展开权限配置 + 弹窗编辑基础信息
- 密钥管理：表格或紧凑卡片 + 展开安全详情 + 弹窗编辑
- 用户管理：表格 + 展开用户详情 + 弹窗编辑角色/套餐

## 设计风格要求

系统管理页属于运维/管理后台，不是营销页面。

建议方向：

- 安静、克制、清晰。
- 信息密度高，但层级明确。
- 适合长期重复操作。
- 操作路径短，按钮位置稳定。
- 不使用大面积 hero、装饰性插画、强营销式卡片。
- 不为了视觉丰富而制造过多颜色和装饰。

视觉上应优先保证：

- 列表可扫描。
- 状态容易判断。
- 表单容易填写。
- 危险操作明确。
- 加载、空态、错误态不突兀。

## 现有前端架构约束

项目遵循 `AGENTS.md` 中的分层规则。

设计落地时应优先复用或优化这些 app 层组件：

- `AppMetricPage`：带标题、说明和顶部指标卡的页面结构
- `AppSectionCard`：页面内区块容器
- `AppSearchBar`：搜索框
- `AppDialog`：新增/编辑弹窗
- `StatusBadge`：状态标签
- `EmptyState`：空态
- `ErrorState`：错误态
- `Notice`：提示信息
- `ToggleField`：开关字段
- `FormField`：通用字段
- `ActionToolbar`：操作按钮组

如果设计需要调整这些元素，优先输出为组件级规范，而不是只给某个页面单独样式。

## 不希望出现的设计方向

请避免：

- 每个页面一套完全不同的卡片结构。
- 在页面内设计一次性特殊组件，无法复用到其他系统管理页面。
- 把系统管理页面做成 landing page 或介绍页。
- 使用过大的标题、过大的留白或装饰性区域降低信息密度。
- 使用太多颜色导致状态语义混乱。
- 将危险操作和普通操作放在同等视觉权重下。
- 在表格、卡片、弹窗中出现文本截断、按钮换行混乱、移动端遮挡。

## 需要设计师重点判断的问题

### 1. 页面主布局

四个页面已确定采用单列管理流。请基于这个方向细化：

- 指标区 + 内容区
- 工具条 + 主列表
- 列表 + 编辑弹窗
- 行内展开详情
- 折叠区配置

不要设计左右分栏或常驻右侧详情。

### 2. 顶部指标卡是否保留

当前页面有顶部 metrics。

请判断：

- 哪些指标对管理操作真正有价值？
- 是否所有页面都需要三个指标？
- 指标卡是否应该更紧凑？
- 指标卡是否应该支持状态色或趋势信息？

### 3. 搜索、筛选和新增按钮

请统一：

- 搜索框位置
- 新增按钮位置
- 筛选入口样式
- 移动端搜索和新增按钮排列
- 列表刷新按钮是否放 header actions

### 4. 列表形态

请判断每个页面最适合：

- 表格
- 卡片列表
- 分组列表
- 权限矩阵

重点关注：

- 信息密度
- 横向空间
- 行操作位置
- hover/selected 状态
- 空态和加载态

### 5. 弹窗表单

新增/编辑弹窗需要统一规范：

- 标题和说明
- 字段分组
- 保存/取消按钮位置
- 危险提示
- 必填和错误提示
- 长表单是否需要分区、tabs 或滚动区域

模型管理的编辑表单字段很多，尤其需要重新分组。

### 6. 状态标签

请定义统一状态色和文案规则。

现有状态包括但不限于：

- active
- inactive
- enabled
- disabled
- visible
- usable
- missing
- pending
- accepted
- failed

需要明确：

- 哪些状态是 success
- 哪些状态是 warning
- 哪些状态是 danger
- 哪些状态是 neutral
- 是否需要中文展示
- 是否保留英文原始状态

### 7. 危险操作

删除、吊销、禁用等操作需要统一：

- 按钮视觉
- 确认弹窗标题
- 确认文案
- destructive 按钮样式
- 成功/失败 toast 文案

### 8. 空态、错误态、加载态

请统一：

- 空态标题
- 空态说明
- 空态是否带动作按钮
- 错误态是否带 retry
- loading skeleton 密度
- refreshing 状态是否保持旧数据

这些会落到 `EmptyState`、`ErrorState`、`DataState`。

## 各页面建议优化方向

### 模型管理建议

建议把编辑表单分为：

- 基础信息
- 上游连接
- 上下文限制
- 能力开关
- 计费和排序

列表中建议突出：

- 模型名
- 上游模型名
- endpoint
- 状态
- 能力标签
- 使用倍率

### 套餐管理建议

建议考虑：

- 套餐表格
- 行内展开模型权限配置
- 或点击“配置权限”打开大弹窗
- 模型权限用更清晰的矩阵或列表

需要重点设计 visible 和 usable 的关系。不可见时可用必须被禁用。

### 密钥管理建议

建议突出：

- secret ref
- 是否已保存密钥
- 状态
- 安全说明

编辑弹窗中需要清楚说明：

- 密钥值不会回显
- 留空表示不修改
- 保存后只显示引用和状态

### 用户管理建议

建议考虑单列用户列表 + 行内展开详情：

- 用户列表
- 展开后展示套餐分配
- 展开后展示月度用量
- 展开后展示权限摘要

需要避免用户、套餐、用量三个信息区混在一起导致优先级不清。

## 设计交付物建议

请设计师尽量交付：

1. 四个页面的 desktop 设计稿。
2. 至少一个页面的 mobile/responsive 方案。
3. 新增/编辑弹窗规范。
4. 状态标签颜色和语义规范。
5. 空态、错误态、加载态示例。
6. 表格/卡片/权限矩阵的组件规范。
7. 危险操作确认弹窗规范。
8. 组件级说明，标注哪些应进入 app 层复用。

本次建议额外输出以下 app 组件设计：

- `AppListToolbar`
- `AppExpandablePanel`
- `AppDisclosureSection`
- `AppFieldGrid`

## 前端落地原则

设计落地时，前端会遵循：

- 优先改 `components/app` 底层组件。
- 页面只组合组件，不重新发明通用 UI。
- 同一类交互只保留一个标准实现。
- API 错误继续走 `normalizeAppError`。
- 用户反馈继续走 `notify`。
- 删除/危险操作继续走 `confirm`。
- 资源加载继续优先使用 `useApiResource` 和 `DataState`。
- 提交前必须通过 `npm run lint` 和 `npm run build`。

## 设计师可以直接回答的问题

为了减少返工，建议设计师在交付时明确回答：

1. 四个页面如何细化同一种单列页面骨架？
2. 顶部指标卡是否保留，保留哪些指标？
3. 列表用表格、卡片还是主从布局？
4. 表单如何分组？
5. 状态颜色和状态文案如何统一？
6. 搜索、筛选、新增、刷新按钮如何摆放？
7. 空态、错误态、加载态如何统一？
8. 哪些视觉规则应该沉淀为 app 层组件？
