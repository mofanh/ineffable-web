# Ineffable Web 国际化迭代计划

## 目标

建立可持续扩展的前端国际化体系，首批支持简体中文（`zh-CN`）和英文（`en-US`）。语言偏好持久化到浏览器，并同步 HTML `lang`、日期、数字和产品文案。

## Phase 1：语言底座与认证入口

- [x] 接入 `i18next` 与 `react-i18next`。
- [x] 建立内置中英文资源、浏览器语言识别和本地偏好持久化。
- [x] 语言变化时同步 `document.documentElement.lang`。
- [x] 迁移登录、注册与认证布局文案。
- [x] 提供键盘可访问的语言切换入口。

验收：登录和注册页面可即时切换中英文，刷新后保持选择，移动端无横向溢出。

验证记录（2026-07-14）：

- `npm run lint`：0 errors / 0 warnings。
- `npm run build`：通过。
- 390px 浏览器回归：默认中文、即时切换英文、刷新保持 `en-US`、HTML `lang` 同步，页面无横向溢出。

## Phase 2：应用 Shell 与通用反馈

- [x] 迁移主导航、Workspace 导航、账号菜单、面包屑和主题入口。
- [x] 迁移 loading、empty、error、confirm、toast 等通用底座默认文案。
- [x] 在登录后的应用 Shell 提供语言切换入口。

验收：登录后可从账号菜单即时切换语言，侧栏、面包屑、主题菜单和通用状态文案同步更新，刷新后保持选择。

验证记录（2026-07-14）：

- `npm run lint`：0 errors / 0 warnings。
- `npm run build`：通过。
- 普通账号真实登录回归：中英文侧栏、面包屑、AI 助手入口、主题菜单和账号菜单即时切换正常。
- 刷新后保持 `en-US`，HTML `lang` 同步；桌面与 390px 均无横向溢出。

## Phase 3：核心工作流

- [x] 迁移 Automation 页面。
- [x] 迁移 Workspace 文件编辑、版本和对象操作。
- [x] 迁移 Gateway Chat、Sandbox、审批和运行事件。

Automation 验证记录（2026-07-14）：

- `npm run lint`：0 errors / 0 warnings。
- `npm run build`：通过。
- 普通账号真实登录回归：英文标题、指标、任务列表、空态与操作入口展示正常，无缺失翻译 key。
- Automation 日期与时间文案跟随当前语言；桌面与 390px 均无横向溢出。

Workspace 验证记录（2026-07-14）：

- `npm run lint`：0 errors / 0 warnings。
- `npm run build`：通过。
- 管理员真实文件回归：英文面包屑、相对更新时间、文件预览、对象操作菜单和版本历史展示正常。
- 未执行写入、恢复或删除操作；桌面与 390px 均无横向溢出，无缺失翻译 key。

Chat / Sandbox 验证记录（2026-07-14）：

- `npm run lint`：0 errors / 0 warnings。
- `npm run build`：通过。
- 管理员真实会话回归：英文历史分组、搜索、新对话、模型选择和 Agent 运行事件展示正常。
- Sandbox 展开时成功刷新 `local-daemon / bound`；审批及流恢复文案已迁移，但未人为触发真实审批与断线流程。
- 桌面与 390px 均无横向溢出，无缺失翻译 key。

## Phase 4：账号、团队与系统管理

- [x] 迁移账号、团队空间、成员、邀请和通知。
- [x] 迁移模型中心与四个系统管理页面。
- [x] 迁移静态兼容页面。

账号 / 团队验证记录（2026-07-14）：

- `npm run lint`：0 errors / 0 warnings。
- `npm run build`：通过；账号与团队页面保持独立懒加载。
- 管理员真实数据回归：英文账号资料、登录会话、团队成员、邀请记录、存储指标和通知空态展示正常。
- 日期和状态文案跟随 `en-US`，桌面页面无横向溢出；未执行邀请、角色更新、成员移除、会话吊销或邀请接受等写操作。

模型 / 系统管理验证记录（2026-07-14）：

- `npm run lint -- --quiet`：通过。
- `npm run build`：通过；模型中心与四个系统管理页面继续独立懒加载。
- 管理员真实数据回归：模型目录、模型管理、套餐管理、密钥管理和用户管理英文展示正常，真实指标与表格数据均成功加载。
- 桌面页面无横向溢出，未执行模型、套餐、密钥、用户或授权的写操作。

## Phase 5：格式化与完整性

- [ ] 日期、数字、相对时间统一使用当前 locale。
- [ ] 建立缺失 key 和中英文资源一致性检查。
- [ ] 完成桌面、390px、明暗主题和真实账号回归。
