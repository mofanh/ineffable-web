# 管理编辑入口统一

Active：套餐展开只读，将模型权限移入编辑窗口，所有修改显式保存。用户详情只读，角色/套餐仅提交差异；部分失败展示进度并支持重试。请求绑定登录会话和编辑代次，加载失败禁止默认值覆盖权限。复用既有接口与表单底座。

验证 lint/i18n/build、相关架构门禁、真实页面浏览器 mock 回归（取消零写入、延迟加载、部分失败重试、跨编辑/会话、保存关闭）。一次集中只读审计及最多一次复审。后端接口不改，无 Rust/release 构建。
